import { Sparkles } from 'lucide-react';

/**
 * Icon used to indicate shiny availability, shared by ShinyChip and PokemonBadge
 */
export const ShinyIndicator = () => (
  <Sparkles className="h-3.5 w-3.5 text-amber-500 dark:text-amber-400" aria-label="Shiny available" />
);
